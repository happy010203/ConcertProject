server:
  port: 8443  # SSL 默認埠
  servlet:
    context-path: /movie
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: ${KEY_STORE_PASSWORD}
    key-store-type: PKCS12
    key-alias: myapp

spring:
  datasource:
    url: jdbc:mysql://34.134.208.114:3306/movie?characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: David
    password: asd123
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  web:
    resources:
      static-locations:
        - classpath:/static/
        - classpath:/public/
        - classpath:/resources/
        - classpath:/META-INF/resources/

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: 
              - email
              - profile
            redirect-uri: "${BASE_URL}/oauth2/callback/{registrationId}"

logging:
  level:
    org.springframework.security: DEBUG

file:
  upload-dir: src/main/resources/static/img 

app:
  base-url: https://localhost:8443/movie  # 更新 base-url 為 HTTPS





